@doc """
    viz(object; [options])

This function wraps `Meshes.viz` for `UlamMethod` objects with `showsegments = true`.
    
The docstring for `Meshes.viz` is provided below.

---    

$(@doc(Meshes.viz))
"""
function viz end

@doc """
    viz!(object; [options])

This function wraps `Meshes.viz!` for `UlamMethod` objects with `showsegments = true`. 
    
The docstring for `Meshes.viz!` is provided below.

---

$(@doc(Meshes.viz!))
"""
function viz! end